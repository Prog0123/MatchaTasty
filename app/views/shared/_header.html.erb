<header class="bg-emerald-600 text-white shadow">
  <div class="w-full px-4 sm:px-6 py-3">
    <div class="flex items-center justify-between">
      <!-- ロゴ -->
      <%= link_to root_path, class: "flex items-center space-x-2 text-xl font-bold" do %>
        <i class="fas fa-leaf"></i>
        <span>MatchaTasty</span>
      <% end %>

      <!-- デスクトップナビゲーション -->
      <nav class="hidden md:flex items-center space-x-4">
        <%= link_to "商品一覧", products_path, class: "hover:text-emerald-200" %>
        <%= link_to "ランキング", rankings_path, class: "hover:text-emerald-200" %>

        <% if user_signed_in? %>
          <div class="relative group">
            <button class="w-9 h-9 bg-emerald-500 rounded-full flex items-center justify-center hover:bg-emerald-400 transition-colors">
              <% if current_user.avatar.attached? %>
                <%= image_tag current_user.avatar, class: "w-full h-full object-cover rounded-full" %>
              <% elsif current_user.image_url.present? %>
                <%= image_tag current_user.image_url, class: "w-full h-full object-cover rounded-full" %>
              <% else %>
                <svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd"></path>
                </svg>
              <% end %>
            </button>

            <!-- デスクトップドロップダウン -->
            <div class="absolute right-0 mt-2 w-40 bg-white text-black rounded-lg shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50">
              <%= link_to "マイページ", mypage_path, class: "block px-4 py-2 hover:bg-gray-100 rounded-t-lg" %>
              <%= link_to "ログアウト", destroy_user_session_path,
                    data: { turbo_method: :delete, turbo_confirm: "ログアウトしますか？" },
                    class: "block px-4 py-2 hover:bg-gray-100 rounded-b-lg" %>
            </div>
          </div>
          
        <% else %>
          <%= link_to "ログイン", new_user_session_path, class: "hover:text-emerald-200" %>
          <%= link_to "新規登録", new_user_registration_path, class: "hover:text-emerald-200" %>
        <% end %>
      </nav>

      <!-- モバイルハンバーガーメニューボタン -->
      <button id="mobile-menu-button" class="md:hidden p-2 rounded-md hover:bg-emerald-500 focus:outline-none focus:ring-2 focus:ring-white">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>

    <!-- モバイルメニュー -->
    <div id="mobile-menu" class="md:hidden hidden mt-4 pb-4 border-t border-emerald-500">
      <div class="flex flex-col space-y-3 pt-4">
        <%= link_to "商品一覧", products_path, class: "block px-2 py-2 hover:text-emerald-200" %>
        <%= link_to "ランキング", rankings_path, class: "block px-2 py-2 hover:text-emerald-200" %>
        
        <% if user_signed_in? %>
          <div class="border-t border-emerald-500 pt-3 mt-3">
            <%= link_to "マイページ", mypage_path, class: "block px-2 py-2 hover:text-emerald-200" %>
            <%= link_to "ログアウト", destroy_user_session_path,
                  data: { turbo_method: :delete, turbo_confirm: "ログアウトしますか？" },
                  class: "block px-2 py-2 hover:text-emerald-200" %>
          </div>
        <% else %>
          <div class="border-t border-emerald-500 pt-3 mt-3">
            <%= link_to "ログイン", new_user_session_path, class: "block px-2 py-2 hover:text-emerald-200" %>
            <%= link_to "新規登録", new_user_registration_path, class: "block px-2 py-2 hover:text-emerald-200" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</header>